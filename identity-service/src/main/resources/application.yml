server:
  port: 8085

spring:
  application:
    name: identity-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: "my-secret-key-jwt"
  expiration: 3600000;